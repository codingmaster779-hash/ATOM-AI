# Atom AI

## How to run locally (VS Code)

1.  `npm install`
2.  `npm run dev`

## How to deploy

### Vercel
1.  Import project.
2.  Deploy. (API Key is hardcoded in geminiService.ts for fail-safe student deployment).

### Netlify
1.  Run `npm run build`.
2.  Drag `dist` folder to Netlify Drop.